# Snow Crew 2.0

MVP Telegram-бот для горнолыжников и сноубордистов: профиль, подбор снаряжения, склоны рядом, поиск компании, события.

## Новое в версии 2.0

### 🎯 Новые фичи
- **Умный матчинг**: сортировка анкет по релевантности (город, уровень, тип катания, расстояние)
- **Фильтры в поиске**: по типу катания и уровню
- **Редактирование профиля**: можно менять отдельные поля без пересоздания
- **Блокировка пользователей**: скрывает из поиска
- **Кто меня лайкнул**: показывает профили заинтересованных
- **Анонимный чат**: общение до мэтча через бота
- **Отзывы на курорты**: рейтинг и комментарии (⭐ 1-5)
- **Уведомления о погоде**: подписка на снег на курортах
- **Рассылка** (админ): /broadcast для сообщений всем пользователям

### 🛠 Технические улучшения
- **Модульная архитектура**: код разбит на handlers/, middlewares/, services/
- **Восстановление FSM**: состояние сохраняется в БД и восстанавливается после перезапуска
- **Rate limiting**: 30 сообщений в минуту
- **Логирование**: в файл и консоль с уровнями и метриками
- **Валидация**: обрезка текста (500 символов), проверка входных данных
- **Новые таблицы БД**: blocks, reviews, chats, chat_messages, weather_subscriptions

## Архитектура

```
bot.py                 — точка входа, background tasks
config.py              — загрузка конфигурации
db.py                  — все SQL-операции (SQLite)
states.py              — FSM состояния
keyboards.py           — Reply/Inline клавиатуры

handlers/              — обработчики по модулям
  ├── __init__.py      — setup_routers()
  ├── common.py        — общие хелперы
  ├── start.py         — /start, навигация, о боте
  ├── profile.py       — профиль (создание/редактирование)
  ├── calculator.py    — калькулятор снаряжения
  ├── resorts.py       — склоны рядом
  ├── buddy_search.py  — поиск компании (с фильтрами)
  ├── events.py        — создание и календарь событий
  ├── contacts.py      — контакты (мэтчи)
  ├── instructors.py   — база инструкторов
  ├── sos.py           — экстренные телефоны
  ├── reviews.py       — отзывы на курорты
  ├── chat.py          — анонимный чат
  └── admin.py         — админ-команды (/stats, /addinst, /broadcast)

middlewares/           — middleware слой
  ├── logging.py       — логирование запросов
  ├── rate_limit.py    — ограничение частоты
  └── state_restore.py — восстановление FSM

services/              — бизнес-логика
  ├── equipment.py     — калькуляторы размеров
  ├── resorts.py       — расчёт расстояний (Haversine)
  └── weather.py       — интеграция с погодным API
```

## База данных

### Таблицы
- `users` — пользователи
- `profiles` — анкеты райдеров
- `likes` — односторонние лайки
- `matches` — взаимные лайки
- `blocks` — блокировки
- `resorts` — 32+ курорта России
- `reviews` — отзывы на курорты
- `events` — групповые выезды
- `event_reminders` — напоминания
- `instructors` — база инструкторов
- `chats` — анонимные чаты
- `chat_messages` — сообщения в чатах
- `weather_subscriptions` — подписки на погоду

### Миграция на PostgreSQL
Весь SQL совместим — заменить `aiosqlite` на `asyncpg` и переиспользовать методы.

## Быстрый старт

### 1. Установка

```bash
git clone <repo>
cd tgbot
pip install -r requirements.txt
```

### 2. Конфигурация

Создай `.env`:

```env
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_PATH=bot.db
WEATHER_API_KEY=ключ_от_openweathermap  # опционально
ADMIN_IDS=123456789,987654321  # через запятую
```

### 3. Запуск

```bash
python bot.py
```

## Команды

### Пользовательские
- `/start` — начало работы
- `/stats` — статистика (только админ)

### Админские
- `/addinst` — добавить инструктора
- `/broadcast` — рассылка всем пользователям

## Логика поиска компании

1. Загружаются профили с учётом фильтров (ride_type, skill_level)
2. Исключаются уже лайкнутые и заблокированные
3. Для каждого профиля рассчитывается **score** (0-100):
   - Тот же тип катания: +20
   - Тот же уровень: +15 (соседний: +5)
   - Тот же город: +20
   - Близкий возраст (<5 лет): +10
   - Близкая геолокация (<10 км): +20
   - Есть описание: +5
   - Есть фото: +10
4. Сортировка по убыванию score
5. Показ по одному с кнопками: лайк, пропустить, чат, блок

## Background Tasks

### Reminder Checker
Проверяет напоминания каждый час:
- Отправляет уведомления за день до события
- Деактивирует старые события (>7 дней)

### Weather Notifier
Проверяет погоду каждый день в 8:00:
- Если на курорте снег → уведомляет подписчиков
- Rate limit: 1 сообщение/сек

## Middleware

### LoggingMiddleware
Логирует каждый запрос:
```
[user_id] message:текст - 25.3ms
[user_id] callback:buddy:like:123 - 45.1ms
```

### RateLimitMiddleware
Ограничение: 30 сообщений / 60 секунд.
При превышении: «⚠️ Слишком много запросов. Подожди немного.»

### StateRestoreMiddleware
Восстанавливает FSM-состояние из БД после перезапуска бота.

## Тесты

```bash
# Установка pytest
pip install pytest pytest-asyncio

# Запуск
pytest tests/
```

## Масштабирование

### Redis для FSM
Заменить хранилище состояний:

```python
from aiogram.fsm.storage.redis import RedisStorage

storage = RedisStorage.from_url("redis://localhost:6379")
dp = Dispatcher(storage=storage)
```

### PostgreSQL
1. Установить `asyncpg`
2. Заменить `aiosqlite.connect()` на `asyncpg.create_pool()`
3. Адаптировать `?` → `$1, $2, ...` в запросах

## Структура коммитов

```
feat: добавлена фича X
fix: исправлен баг Y
refactor: рефакторинг Z
docs: обновлена документация
```

## Roadmap (Future)

- [ ] WebApp для профилей
- [ ] Голосовые сообщения в чате
- [ ] Карта курортов на яндекс.картах
- [ ] Интеграция с камерами курортов
- [ ] Автоопределение уровня по фото/видео (ML)

## Контакты

Разработчик: @aleblanche  
Версия: 2.0  
Лицензия: MIT
